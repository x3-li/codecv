高同時実行条件（60超）では、システム全体の待ち時間要因が大きくなり、通常利用シナリオとかけ離れた挙動となるため、本評価対象から除外した。

---

Output token速度が低く、単一リクエスト遅延が大きいため、ユーザー体験を考慮し対象外とする。

---
FP8とBF16で性能差は小さく、KV cache使用量も十分確保できているため、今後はFP8を使用しない方針とする。

---

---


协助我完成一个简单的load balance系统架构设计，根据我的提示，仔细思考设计方案。

我有2台Dgx Spark物理机器， 不使用roce高速连接， 两台机器都是用docker 启动vllm 挂载相同的model。

还有一台普通liunx vm机器， 当作load balance物理机， 下面是我想要的方案，但是我没有深入了解litellm能不能做这件事。

litellm 当作nginx的load balance使用， 调用两个vllm是轮询的， 其中一个vllm停止也不会导致chat功能不能用



---


Linux vm
  - docker 
  - litellm
  - client OpenWebUI
  - load balance config

dgx 1
  - docker 
  - vllm 加 model

dgx 2
  - docker 
  - vllm 加 model


--

换个风格， 最好是类似于excalidraw风格的系统设计图

要体现出， 用户通过浏览器 -> Openwebui (Linux vm) -> litellm  (Linux vm) -> dgx 1/ dgx2
而且整体图片上的文字描述要用日语


图片中要清晰的展现这个构造
```
Linux vm
  - docker 
  - litellm
  - client OpenWebUI
  - load balance config

dgx 1
  - docker 
  - vllm 加 model

dgx 2
  - docker 
  - vllm 加 model
```

SVG 动态矢量图：litellm 负载均衡到两台 dgx 服务器，带流量小点沿路径移动。
